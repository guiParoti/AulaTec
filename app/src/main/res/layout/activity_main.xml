<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- ========================= -->
    <!--  LAYOUT INTERNO PRINCIPAL -->
    <!-- ========================= -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#040101">

        <!-- ========================= -->
        <!--        LOGO CENTRAL       -->
        <!-- ========================= -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="336dp"
            android:layout_height="391dp"
            android:visibility="visible"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.493"
            app:srcCompat="@drawable/logoaulatec"
            tools:srcCompat="@drawable/logoaulatec" />

        <!-- ========================= -->
        <!--       ÍCONE INSTAGRAM     -->
        <!-- ========================= -->
        <ImageView
            android:id="@+id/iconInsta"
            android:layout_width="57dp"
            android:layout_height="46dp"
            android:layout_marginStart="4dp"
            android:layout_marginBottom="48dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/linkGuilherme"
            app:layout_constraintHorizontal_bias="1.0"
            app:srcCompat="@drawable/insta" />

        <!-- ========================= -->
        <!--        LINK GUILHERME     -->
        <!-- ========================= -->
        <TextView
            android:id="@+id/linkGuilherme"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="240dp"
            android:layout_marginBottom="60dp"
            android:clickable="true"
            android:focusable="true"
            android:text="guilhermeparoti"
            android:textColor="#FBF7F7"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
